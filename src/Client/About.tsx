import { Typography } from "@suid/material";

const text = ""

export default function About() {
    return (
        <main>
            <Typography variant="h3">Hei og god jul!</Typography>

            <Typography>
                Denne siden startet med at jeg fikk en ide om at det hadde vært kult å bygge et verktøy for å lagre statistikk om gården.
                
            </Typography>
            

        </main>

    );
}